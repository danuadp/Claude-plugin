---
description: Greet the user by name. Example skill demonstrating $ARGUMENTS — safe to delete.
disable-model-invocation: true
---

# Hello

Greet the user named "$ARGUMENTS" warmly, then ask what they'd like to work on.

If `$ARGUMENTS` is empty, greet them without a name instead of saying "Hello, !".
