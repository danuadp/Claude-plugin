interface:
  display_name: "Claude Plugin"
  short_description: "Repo-specific patterns and workflows for Claude-plugin"
  default_prompt: "Use the Claude-plugin repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true